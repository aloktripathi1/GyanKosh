VITE_API_KEY=dev-local-key
# Leave unset for local dev (Vite's server.proxy in vite.config.js handles /api).
# Must be set to the deployed backend's real URL for any production build,
# e.g. https://gyankosh-backend.onrender.com
VITE_API_BASE_URL=
